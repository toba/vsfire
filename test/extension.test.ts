import * as assert from "assert";
//import * as vscode from "vscode";
//import { suggestion } from "../src/providers/parser";

suite("Completions Providers", () => {
   test("Math suggestions", ()=> {
      assert(true, "fake test");
   });
});